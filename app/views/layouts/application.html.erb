<!DOCTYPE html>
<html lang="en">
<head>
  <title>SimpleRPG</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= stylesheet_link_tag "bootstrap.min.css", :media => "all" %>
  <%= stylesheet_link_tag "bootstrap-responsive.css", :media => "all" %>

  <%= javascript_include_tag "application" %>
  <%= javascript_include_tag "bootstrap.min.js" %>

  <%= csrf_meta_tags %>
</head>
<body>
	<div class="navbar navbar-fixed-top">
		<div class="navbar-inner">
    		<div class="container">
				<a class="brand" href="#">Simple RPG</a>
				<ul class="nav">
					<% if current_user %>
						<li><p class="navbar-text" style="margin-top:-2px">Logged in as <%= current_user.username %></p></li>
						<li><%= link_to "Log out", logout_path %></li>
					<% else %>
						<li><%= link_to "Sign up", signup_path %></li>
						<li><%= link_to "log in", login_path %></li>
					<% end %>
				</ul>
			</div>
		</div>
	</div>

	<% flash.each do |name, msg| %>
	<%= content_tag :div, msg, :id => "flash_#{name}" %>
	<% end %>

<%= yield %>
</body>
</html>
